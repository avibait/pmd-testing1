public with sharing class testingPMD1 {
    public void testingPMD1() {
        String s = 'hello';

        Boolean hi;
        String str;

        
       List<Account> accList = [Select Name From Account];
       for (Integer x = 0; x < 10; x++){
           Account acc = [select name from account];
    }
}
